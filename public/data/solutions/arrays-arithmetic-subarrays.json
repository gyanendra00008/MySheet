{"id": "arrays-arithmetic-subarrays", "globalNo": 4, "order": 4, "title": "Arithmetic Subarrays", "fullTitle": "Arithmetic Subarrays", "chapterId": "arrays", "chapterName": "Arrays", "topicId": "arrays-core", "topicName": "Core & Practice", "difficulty": "Unknown", "leetcodeUrl": "https://leetcode.com/problems/arithmetic-subarrays", "gfgUrl": null, "youtubeUrl": "https://www.youtube.com/watch?v=ErHay1GfIqw", "companyTags": ["Atlassian Online Assessment for Females"], "timeComplexity": "O(m", "spaceComplexity": "O(n) creating arrays", "hasCpp": true, "hasJava": true, "sourcePath": "Arrays/Arithmetic Subarrays.cpp", "code": "/*\n      MY YOUTUBE VIDEO ON THIS Qn : https://www.youtube.com/watch?v=ErHay1GfIqw\n      Company Tags                : Atlassian Online Assessment for Females\n      Leetcode Link               : https://leetcode.com/problems/arithmetic-subarrays\n*/\n\n/***************************************************************** C++ ******************************************************************************/\n//Approach-1 (using simple sorting and check)\n//T.C : O(m*nlogn)\n//S.C : O(n) creating arrays\nclass Solution {\npublic:\n    bool check(vector<int> &arr) {\n        sort(begin(arr), end(arr));\n        int n = arr.size();\n        \n        int d = arr[1] - arr[0];\n        \n        for(int i = 2; i<n; i++) {\n            if(arr[i] - arr[i-1] != d) {\n                return false;\n            }\n        }\n        return true;\n    }\n    \n    vector<bool> checkArithmeticSubarrays(vector<int>& nums, vector<int>& l, vector<int>& r) {\n        int m = l.size();\n        \n        vector<bool> result;\n        \n        for(int i = 0; i<m; i++) {\n            \n            vector<int> arr(begin(nums) + l[i], begin(nums) + r[i] + 1);\n            \n            result.push_back(check(arr));\n            \n        }\n        \n        return result;\n    }\n};\n\n\n//Approach-2 (Without sorting)\n//T.C : O(m*n)\n//T.C : O(n) using hash set\nclass Solution {\npublic:\n    bool check(vector<int> &arr) {\n        int n = arr.size();\n        int minElement = INT_MAX;\n        int maxElement = INT_MIN;\n        unordered_set<int> st;\n        \n        for (int &num : arr) {\n            minElement = min(minElement, num);\n            maxElement = max(maxElement, num);\n            st.insert(num);\n        }\n        \n        if((maxElement-minElement) % (n-1) != 0)\n            return false;\n        \n        int diff = (maxElement-minElement)/(n-1);\n        int curr = minElement + diff;\n        \n        \n        \n        while(curr < maxElement) {\n            if(st.find(curr) == st.end()) {\n                return false;\n            }\n            curr += diff;\n        }\n        \n        return true;\n    }\n    \n    vector<bool> checkArithmeticSubarrays(vector<int>& nums, vector<int>& l, vector<int>& r) {\n        int m = l.size();\n        \n        vector<bool> result;\n        \n        for(int i = 0; i<m; i++) {\n            \n            vector<int> arr(begin(nums) + l[i], begin(nums) + r[i] + 1);\n            \n            result.push_back(check(arr));\n            \n        }\n        \n        return result;\n    }\n};\n\n\n/***************************************************************** JAVA **************************************************************************/\n//Approach-1 (using simple sorting and check)\n//T.C : O(m*nlogn)\n//S.C : O(n) creating arrays\npublic class Solution {\n    public boolean check(List<Integer> arr) {\n        arr.sort(null);\n        int n = arr.size();\n\n        int d = arr.get(1) - arr.get(0);\n\n        for (int i = 2; i < n; i++) {\n            if (arr.get(i) - arr.get(i - 1) != d) {\n                return false;\n            }\n        }\n        return true;\n    }\n\n    public List<Boolean> checkArithmeticSubarrays(int[] nums, int[] l, int[] r) {\n        int m = l.length;\n\n        List<Boolean> result = new ArrayList<>();\n\n        for (int i = 0; i < m; i++) {\n            List<Integer> arr = new ArrayList<>();\n            for (int j = l[i]; j <= r[i]; j++) {\n                arr.add(nums[j]);\n            }\n\n            result.add(check(arr));\n        }\n\n        return result;\n    }\n}\n\n\n\n//Approach-2 (Without sorting)\n//T.C : O(m*n)\n//T.C : O(n) using hash set\npublic class Solution {\n    public boolean check(List<Integer> arr) {\n        int n = arr.size();\n        int minElement = Integer.MAX_VALUE;\n        int maxElement = Integer.MIN_VALUE;\n        Set<Integer> st = new HashSet<>();\n\n        for (int num : arr) {\n            minElement = Math.min(minElement, num);\n            maxElement = Math.max(maxElement, num);\n            st.add(num);\n        }\n\n        if ((maxElement - minElement) % (n - 1) != 0) {\n            return false;\n        }\n\n        int diff = (maxElement - minElement) / (n - 1);\n        int curr = minElement + diff;\n\n        while (curr < maxElement) {\n            if (!st.contains(curr)) {\n                return false;\n            }\n            curr += diff;\n        }\n\n        return true;\n    }\n\n    public List<Boolean> checkArithmeticSubarrays(int[] nums, int[] l, int[] r) {\n        int m = l.length;\n\n        List<Boolean> result = new ArrayList<>();\n\n        for (int i = 0; i < m; i++) {\n            List<Integer> subArray = new ArrayList<>();\n            for (int j = l[i]; j <= r[i]; j++) {\n                subArray.add(nums[j]);\n            }\n            result.add(check(subArray));\n        }\n\n        return result;\n    }\n}\n"}